import { type Config } from "prettier";

const config: Config = {
  singleQuote: true,
  trailingComma: "all",
  printWidth: 120,
  tabWidth: 2,
  useTabs: false,
  semi: true,
  arrowParens: "always",
};

export default config;